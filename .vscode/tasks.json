{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-run",
            "type": "shell",
            "command": "go build -o ${workspaceFolder}/zyron ${workspaceFolder} && ${workspaceFolder}/zyron",
            "isBackground": false,
            "problemMatcher": []
        }
    ]
}
